### 변수 선언
## var, let으로 선언할 경우 값을 추후에 변경할 수 있다.
## const로 선언한 경우 값 변경을 할 수 없다.
## var는 유지보수에만 사용하고 더이상 사용하지 않는다.


## let 변수를 선언만 하고 값은 할당하지 않거나 나중에 추가할 수도 있다.
## const는 변수 선언만 하고 값을 할당하지 않으면 에러가 나고 실행도 되지 않는다.
